{
  "$schema": "https://json.schemastore.org/tsconfig",
  "extends": "./tsconfig.base.json",
  "compilerOptions": {
    "composite": false
  },
  "include": ["libs", "services", "docs", "runbooks", "apps", "contracts"],
  "exclude": ["node_modules", "dist"]
}
