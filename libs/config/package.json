{
  "name": "@plutus/config",
  "version": "0.1.0",
  "description": "Centralized configuration loader with residency-aware defaults for Plutus services",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "test": "jest --config ./jest.config.ts --runInBand",
    "lint": "eslint 'src/**/*.{ts,tsx}' --max-warnings=0",
    "format": "prettier --write 'src/**/*.{ts,tsx,json,md}'"
  },
  "dependencies": {
    "dotenv": "^16.4.5"
  },
  "devDependencies": {
    "@types/node": "^20.19.9",
    "ts-jest": "^29.1.1",
    "typescript": "^5.4.2"
  }
}
