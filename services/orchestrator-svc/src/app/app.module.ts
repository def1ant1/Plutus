import { Module } from '@nestjs/common';

/**
 * Minimal Nest module used to bootstrap the orchestrator service. Domain-specific
 * providers and controllers should be registered here as the service evolves.
 */
@Module({})
export class AppModule {}
